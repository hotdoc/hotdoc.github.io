fragment_downloaded_cb({"fragment": "core.extension.Extension.get_or_create_symbol \nExtensions that discover and create instances of core.symbols.Symbol should do this through this method as it will keep an index of these which can be used when generating a naive index \nSee core.database.Database.get_or_create_symbol for more information. \n", "url": "extension.html#core.extension.Extension.get_or_create_symbol"});