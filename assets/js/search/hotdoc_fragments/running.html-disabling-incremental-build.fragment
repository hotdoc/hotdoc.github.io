fragment_downloaded_cb({"url": "running.html#disabling-incremental-build", "fragment": "Disabling incremental build \nBy default rebuild is incremental which vastly reduces the time needed to build the documentation. \nAs this is still an experimental feature if you wish to rebuild everything at each run you can simply clean hotdoc s output beforehand please file a bug if you have any reason to do that though. \n"});